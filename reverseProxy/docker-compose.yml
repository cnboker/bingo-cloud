version: '3.3'

services:
   reverseproxy:
     container_name: reverseproxy
     restart: always
     ports:
      - "80"
     networks:
       - "myNetwork"
networks:
  myNetwork:
    external: true